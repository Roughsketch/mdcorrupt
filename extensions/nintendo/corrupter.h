#ifndef _NINTENDO_CORRUPTER_H
#define _NINTENDO_CORRUPTER_H

/*
#include <vector>
#include <cstdint>

template<typename F>
class NintendoCorrupter
{
public:
  NintendoCorrupter(std::function<void(std::vector<uint8_t>&)>&, F &);

  inline bool is_match(std::vector<uint8_t>& data)
  {
    return m_match(data);
  }
private:
  std::function<void(std::vector<uint8_t>&)> m_corrupt;
  F m_match;
};
*/
#endif
